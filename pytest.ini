[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -m "not e2e"
markers =
    integration: marks tests as integration tests (may hit external APIs)
    e2e: marks tests as end-to-end tests (require network access)
